{{!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
</head>
<body>
    <div class="container">
        {{#if userdata}}
        <div>Halo {{userdata.name}}</div>
        {{/if}}
        <div>
            <a href="/experience">experience</a>
        </div>
        <div>
            <a href="project">project</a>
        </div>
        <div>
            <a href="technology">technology</a>
        </div>
    </div>

</body>
</html> --}}

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{title}}</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gradient-to-r from-gray-100 to-gray-200 min-h-screen font-sans">

    <!-- Navigation Bar -->
    <nav class="bg-white shadow-md px-6 py-4 flex justify-between items-center">
        <div class="flex items-center gap-3">
            <span class="text-2xl pl-4 font-bold text-gray-800">Dashboard</span>
        </div>
        {{#if userdata}}
        <div class="flex items-center gap-4">
            <span class="text-gray-700 text-lg">Halo, <strong>{{userdata.name}}</strong></span>
            <form action="/logout">
                <button class="bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600 transition">Logout</button>
            </form>
        </div>
        {{else}}
        <div class="flex items-center gap-4">
            <form action="/login">
                <button class="bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600 transition">Login</button>
            </form>
        </div>
        {{/if}}
    </nav>

    <!-- Content Area -->
    <div class="container mx-auto mt-12 px-4">
        <div class="grid gap-8 grid-cols-1 sm:grid-cols-3 text-center">

            <a href="/experience"
                class="bg-white aspect-square p-6 flex flex-col justify-center items-center rounded-xl shadow-md hover:shadow-xl transform hover:scale-105 transition duration-300">
                <h2 class="text-2xl font-bold text-gray-800 mb-2">📌 Experience</h2>
                <p class="text-gray-500 text-sm">career journey</p>
            </a>

            <a href="/project"
                class="bg-white aspect-square p-6 flex flex-col justify-center items-center rounded-xl shadow-md hover:shadow-xl transform hover:scale-105 transition duration-300">
                <h2 class="text-2xl font-bold text-gray-800 mb-2">🚀 Project</h2>
                <p class="text-gray-500 text-sm">my project</p>
            </a>

            <a href="/technology"
                class="bg-white aspect-square p-6 flex flex-col justify-center items-center rounded-xl shadow-md hover:shadow-xl transform hover:scale-105 transition duration-300">
                <h2 class="text-2xl font-bold text-gray-800 mb-2">💡 Technology</h2>
                <p class="text-gray-500 text-sm">mastered technology</p>
            </a>

        </div>
    </div>

</body>

</html>